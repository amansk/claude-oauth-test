{
  "name": "claude-oauth-test",
  "version": "1.0.0",
  "description": "OAuth proxy test for Claude MCP connector",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "node-fetch": "^2.6.0"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
